var GOOGLE_API_DEMAND_POLICY_VERSION='GOOGLE_API_DEMAND_POLICY_V1_20260822';
function evaluateGoogleApiNeed_(ctx){ctx=ctx||{};var api=String(ctx.api||'');var allowed=['drive.googleapis.com','sheets.googleapis.com','texttospeech.googleapis.com','youtube.googleapis.com','docs.googleapis.com'];if(allowed.indexOf(api)<0)return{ok:true,useApi:false,reason:'API_NOT_ALLOWED_OR_NOT_REQUIRED',version:GOOGLE_API_DEMAND_POLICY_VERSION};if(ctx.freshCache===true&&ctx.frontRequirementPass===true&&ctx.externalSignalDue!==true)return{ok:true,useApi:false,reason:'FRESH_REUSE',version:GOOGLE_API_DEMAND_POLICY_VERSION};var signal=String(ctx.signal||'').toUpperCase();var due=['NEW','RECENT','TREND','AUDIO_ASSET_GAP','PUBLISH_METADATA_GAP','COVERAGE_GAP'].indexOf(signal)>=0;var use=due||ctx.externalSignalDue===true||ctx.frontRequirementPass===false;return{ok:true,useApi:use,reason:use?'DEMAND_GAP_OR_EXTERNAL_SIGNAL':'NO_DEMAND',signal:signal,version:GOOGLE_API_DEMAND_POLICY_VERSION};}
function auditGoogleApiDemandPolicy(){return{ok:true,version:GOOGLE_API_DEMAND_POLICY_VERSION,frontApiKey:'FORBIDDEN',triggerPolicy:'CHECK_DUE_THEN_CALL',alwaysOnLane:['NEW','RECENT','TREND']};}
function testGoogleApiDemandPolicyX2(){var a=evaluateGoogleApiNeed_({api:'texttospeech.googleapis.com',freshCache:true,frontRequirementPass:true,signal:'NONE'});var b=evaluateGoogleApiNeed_({api:'youtube.googleapis.com',freshCache:false,frontRequirementPass:true,externalSignalDue:true,signal:'RECENT'});return{ok:a.useApi===false&&b.useApi===true,reuse:a,recent:b,version:GOOGLE_API_DEMAND_POLICY_VERSION};}
